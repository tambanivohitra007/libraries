{
  "name": "@rindra/libraries",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI libraries extracted from repeated project code.",
  "scripts": {
    "new": "pnpm --filter @rindra/create-app build && node packages/create-app/dist/index.js",
    "gallery": "pnpm --filter @rindra/gallery dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@11.5.1"
}
